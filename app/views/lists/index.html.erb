<h1>Lists#index</h1>
<p>Find me in app/views/lists/index.html.erb</p>

<tbody>
  <% @lists.each do |list| %>
    <tr>
      <td><%= link_to list.id, lists_path(list) %></td>
      <td><%= list.name %></td>
      <td><%=l list.created_at %></td>
    </tr>
  <% end %>
</tbody>
