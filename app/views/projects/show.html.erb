<h1>Projects#show</h1>
<p>Find me in app/views/projects/show.html.erb</p>

<%= @project.name %>:::::<%= @project.is_public %>

<% form_for(@project) do |f| %>
<%= f.radio_button :is_public, true %> <%= f.label :is_public, "Yes" %>
<%= f.radio_button :is_public, false %> <%= f.label :is_public, "No" %>
<% end %>
